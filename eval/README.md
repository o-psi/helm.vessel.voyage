# Evaluation status

The previous broad automated suites, evaluation scenarios and evaluation runner
were removed at the operator's request. Their recreation is separate work, not
a prerequisite for using the existing focused checks. Retained historical evidence
does not validate the current source tree or target architecture.

The [quality guide](../docs/quality.md) lists focused checks and their evidence limits;
verify command availability in the selected checkout before running them.
The [implementation sequence](../docs/implementation.md) identifies behavior that
needs evidence as the new runtime boundaries are delivered. Live provider work
requires a separately approved provider and budget.

The current [readiness direction](../docs/readiness.md) requires no human testing,
visual acceptance, trial schedule or incumbent cutover gate yet. Historical trial
proposals do not supersede that direction or turn unrun checks into passes.
See [useful-work evidence](../docs/useful-work-evidence.md) for #22's objectively
checked outputs, retained failures and unresolved live/deployment scope.
