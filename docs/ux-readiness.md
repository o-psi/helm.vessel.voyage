# Helm UX readiness

**Current verdict: incomplete executable coverage, not whole-product certification.**
[#14](https://github.com/o-psi/helm.vessel.voyage/issues/14) tracks the interface
journeys below. The current operator instruction explicitly removes human testing,
product-owner visual sign-off and adoption/cutover as prerequisites for this work.
Historical review requirements remain history, not closure gates. This waiver does
not turn unimplemented or unexercised workflows into passing evidence.

Use objective executable checks against the current **Helm → Vessel → independent
Voyage** architecture. Record exact source/binary identities, meaningful adverse
outcomes and limitations. Build/package checks alone do not establish functional
journey correctness. Historical evidence remains scoped in
[implementation](implementation.md); current focused checks are in [quality](quality.md).

## What counts as complete

A journey starts with a user intention and ends with an understandable outcome or
an actionable recovery path. A command handler or successful API response alone
is not a usable journey. This checklist is internal to development and review.
The product should expose user intentions, names, choices and outcomes rather than
UUIDs, revisions, protocol fields, schemas or a diagnostic checklist. Engineering
details belong in explicit developer diagnostics, not the everyday interface. The user must be able to discover the action, identify its
target, understand where input goes, observe progress, and recover without copying
implementation identifiers or guessing whether an effect happened.

For each first-release journey, account for these six criteria:

| Criterion | Acceptance |
| --- | --- |
| Discoverability | A new user can find it from the relevant screen or contextual help without being coached. A slash command alone is insufficient for a primary task. |
| Target and focus | The voyage, executing host and input destination are visible. Switching, delayed responses and resize cannot silently redirect an action. |
| Outcome | Distinguish requested, admitted, working, awaiting input, completed, failed and unknown. Cancellation requested is not observed cleanup. |
| Recovery | Explain what happened, what is retained, what the user can do next, and whether retry is safe. Preserve drafts and unresolved command identities. |
| Presentation | Readable hierarchy, field labels and selection; no wire JSON dumps, replacement glyphs generated by the renderer, inaccessible overflow or hidden essential controls. Deliberately authored code examples retain their meaning. |
| Verification | Exact-build executable assertions and representative rendered frames establish the claimed functional slice. Record failures and unexercised states. No human or product-owner sign-off is required for the current work. |

No average score overrides a failed authority, privacy, data-loss or ambiguous-effect
criterion. A polished happy path cannot compensate for an unusable recovery path.

## Inventory from the current code

Stable journey IDs let issues, captures and release records refer to the same scope.
The table inventories current entrypoints, not approved release exclusions. Owners below identify implementation responsibility, not permission to discard a requirement.
**Review** means a surface exists but its full objective scenario coverage has not been recorded.
**Gap** means the current primary task relies on an absent or insufficient TUI path.
**Partial evidence** identifies a locally exercised slice, not a shipping approval.
Every row needs a supported-scope decision and concrete verification; human review is not a current prerequisite.

| ID | User intention | Current entrypoint | Readiness / missing acceptance |
| --- | --- | --- | --- |
| J01 | Start Helm and understand background work | `helm`, `helm chat`, `helm connect` | Review: first launch, missing Vessel, launch failure, empty catalogue and independent runtime lifetime. |
| J02 | Connect to the intended machine | Vessels panel, Ctrl+G and `/vessels`; remembered local WS/remote WSS connections | Implemented #194; failed probes pause the route and explicit Connect / Retry resumes observations without replay. Credential protection/audit remains #10; broader disclosure #79. [Connection evidence](vessel-connections-verification.md) predates the current duplex transport. |
| J03 | Configure a provider and sign in | Composer Account / `/account`, private native device sign-in, executing-host `vessel auth accounts` for API enrollment | Named executing-host accounts and guarded local/remote selection delivered in #213; API keys remain in the executing-host private CLI, never ordinary chat. Private account controls require 44×22; smaller layouts explain resizing and permit Escape. [Account guide](provider-accounts.md). Credential protection/audit remains #10/#200. |
| J04 | Create a voyage in a workspace | Ctrl+N, `/new`, approved-workspace picker and local draft | First send creates an independent owner; plain startup and draft editing do not. Exact pending creation/turn identities are retained and resolved without replay. Review actual failure and duplicate-send cases. |
| J05 | Find, resume and switch voyages | F2 searchable named voyage/draft picker at all supported widths; Tab/Shift+Tab; sidebar | Picker captures identities, searches names/hosts/status/workspaces, preserves composers and permits navigation away from pending decisions without answering them. No UUID entry required. F5 selects archives; F2 then searches that inventory. |
| J06 | Understand host, model, run and observation state | Header, voyage list, snapshot errors | Review: stale/offline versus idle, stopped versus completed, duplicate routes, long names and many voyages. |
| J07 | Write, edit and recall a message | Composer, Enter, Alt+Enter, arrows, prompt history | Review: multiline cursor, graphemes, paste, limit errors and disk-write failure; input and display must remain aligned. |
| J08 | Send a turn and follow its answer | Enter, `connect submit/run/chat`, `run` | Review: admission versus completion, streaming interruption, tool failure, provider refusal and first meaningful feedback. Live provider evidence is separate. |
| J09 | Steer work already running | Enter during an active run | Review: queued/applied/not-applied explanation, operator-run refusal and last-moment completion races. |
| J10 | Read conversation and code | Markdown view, Ctrl+T activity, Ctrl+F find, PageUp/PageDown, Ctrl+End | #150 adds faithful authored content, word wrapping, paragraph spacing, activity and anchored reading. Exact-build adverse reading scenarios remain required; no human sign-off gate. |
| J11 | Read older or oversized history | Ctrl+Home earlier pages, automatic full-message loading, `/export` | #150 adds revision-bound TUI loading (16,384 messages / 64 MiB). Search is limited to loaded rows; uncached stopped archives require restoration. Larger-window and slow-route acceptance remain separate. |
| J12 | Resolve uncertain submission | Pending delivery presentation and automatic reconciliation; optional `/receipt` diagnostics | Original public envelope/ID retained; status checks never replay an effect. Definite non-admission restores editing, unknown stays fenced. No manual receipt-ID copying required. |
| J13 | Cancel work and see cleanup | Voyage Actions (F9), `/cancel` | Exact observed run; pending cancellation is not observed cleanup. Review repeat actions, route loss and retained cleanup. |
| J14 | Review an execution approval | Automatic request view, Up/Down, Enter, Esc | Partial evidence #151: automatic focus, visible denial/allow-once choices, keyboard denial prevented the local command; explicit allowance executed it. Expiry, unsupported kinds, stale identity, repeat keys and wrong-voyage protection still need full matrix acceptance. |
| J15 | Answer or skip a model question | Automatic request view, options/custom editor, Enter, Esc | Partial evidence #151: real operator questions, option selection, Escape skip, custom editor/back/send and preserved composer; captures at 40x18, 80x24 and wide sizes. Paste and expiry matrix still needs acceptance. |
| J16 | Notice and navigate simultaneous requests | Sidebar state, automatic request view, Left/Right | Review: multiple requests and voyages, focus handoff, request disappearance, detail overflow, timeout while typing. |
| J17 | Find an interactive program | Persistent F3, `/terminals`, named browser | Partial evidence #143: local retained-terminal inventory and wide/narrow browser. No password-prompt inference is claimed. |
| J18 | Enter private program input and return | Browser Enter; private console Ctrl+] | Partial evidence #143: local input, blank/wide cells, cursor and return to draft; dummy input absent from durable Vessel files. Full terminal-app compatibility is not established. |
| J19 | Recover an unavailable or ended terminal | Inventory state and attachment errors | Review: removed terminal, exited program, stale observation, restart, revocation, resize and uncertain write. Do not replay private input. |
| J20 | Understand available tools and run an action | F8 → Run a tool; live-schema action/typed fields and review | Public arguments only; runtime validates registry/policy. Named task/agent references, enum/boolean choices and lists do not require raw JSON/UUID entry. Unsupported/private/nested schema fields remain explicitly unavailable, not silently coerced. |
| J21 | Understand and change access policy | Composer Access, F8 → Access, Actions → Access; `/policy` details | Read only / Ask first / Unrestricted already have target-bound confirmation and runtime enforcement. Broader structured policy editing is a real remaining #14/#70 gap: current full Configure takes an executing-host path, not a typed patch/preview contract. |
| J22 | Follow and manage tasks | F8 → Manage tasks; typed `todo` actions with named references; `/todos` overview | Create/edit/status/blockers/dependencies/assignments/evidence use the actual tool and durable operator path. Progress and evidence remain distinct; refusal preserves the composer. Large or incomplete inventories are disclosed. |
| J23 | Supervise subordinate agents | F8 → Delegated work; typed `subagent` actions with named references; `/subagents` overview | Named status/message/cancel/worktree actions retain exact target identity. Cancellation requested is not observed cleanup; the first 100 archived agents are also selectable by name. Incomplete/older-owner archive discovery is disclosed; never reuse an archived identity. |
| J24 | Discover and run a saved workflow | F8 → Saved workflows; `/workflows` opens the same flow | Named executing-host definitions, explicit digest trust, typed inputs, executing-host preview and separate submit. Secret values use transient masked input, never composer/durable public envelopes; lost results reconcile without replaying private input. |
| J25 | Select/change inference settings | Clickable Model/Thinking/Service, `/model`, `/thinking`, `/service`; F8 Models | Searchable selectors implemented #177/#182. Review inherited/default/explicit values and next-turn versus active settings; metadata is not live entitlement or billing certification. |
| J26 | Rename or branch a voyage | Voyage Actions (F9), `/rename`, `/branch` | Named editor separate from composer; source draft remains associated with its source. Review stale revision, failed/unknown branch creation and retained child identity. |
| J27 | Archive and restore | Voyage Actions (F9); F5 Archives; F2 named search | Archive preserves history and observes runtime cleanup; restore retains the same UUID. Uncached stopped archive history needs restoration. |
| J28 | Clear, compact or delete history | Voyage Actions (F9) → Clear conversation / Compact older messages / Delete permanently | Named target and scope disclosure, explicit CLEAR / KEEP count / DELETE, current-history revision check and retained composer. Compaction omits content with a marker, not generated summarization. Deletion is application deletion, not forensic erasure or recall of external copies. |
| J29 | Export a complete conversation | `/export PATH`, `connect export` | Review: destination collision, permissions, mid-export revision change, disk failure and visible resulting path. No private input export. |
| J30 | Disconnect and reconnect | Ctrl+C/Ctrl+Q, `/quit`, reconnect commands | Partial evidence #143: normal local detach/draft return. Slow route, broken transport and outstanding mutations remain separate cases. |
| J31 | Continue after runtime failure | Automatic fenced recovery on history/turn request; technical stop/restart/admin controls | Normal continuation no longer requires receipt diagnostics or cleanup attestations. Missing process evidence remains unknown; retained external effects are not replayed. Advanced service/owner administration still needs a dedicated UI contract (#14/#77). |
| J32 | Understand resource and completion limits | F8 This machine, current run/cleanup status and named cancel/actions | Current data describes cleanup/accounting, not a CPU/cost dashboard. Telemetry/pricing/budget contracts remain #71; do not invent totals or restore superseded per-item completion sign-off. |
| J33 | Review and change sharing/access | Scoped access files, protocol grants/revocation, administration | Gap: no first-class TUI scope/share/revoke flow. Verify observe-only denial and executing-host credentials. See #79. |
| J34 | Trust a Vessel and move an owner | `connect admin identity/trust/move` | Gap: trust/file/courier CLI; source/destination, interrupted transfer and single-owner recovery need designed screens. |
| J35 | Add/remove participants and reconcile assignments | `connect admin accept-participant/remove-participant/assignment`, runtime tools | Gap: no participant/assignment workspace; unknown admission must block unsafe reassignment. See #77. |
| J36 | Retired outbound worker mode | None | Removed from first-release scope under #9; human connections and participant execution retain their own authority. |
| J37 | Import legacy sessions and inspect managed work | `connect import`, `managed`, resume/import paths | Review/gap: expert migration inputs, fingerprint/source conflict, partial migration and original-history preservation. |
| J38 | Configure extensions, MCP, onboarding and inference allowances | `extension`, `onboard`, `inference`, configuration/runtime tools | Gap: no complete integrated discovery/configuration/revocation UI. Expand each advertised subcommand before claiming family-wide readiness. |
| J39 | Review and publish GitHub work | `github`, runtime publication decisions | Review/gap: exact content/target review and lost publication response across CLI/runtime interaction surfaces. |
| J40 | Get help and diagnose an unavailable command | F1, `/help`, `--help`, `doctor`, manpage/completions | Partial evidence #143: guide and scrolling. Unknown commands, no selected voyage, disabled capabilities and next-step errors need review. |
| J41 | Install, upgrade, roll back and manage the user service | `voyage-installer`, installer guide | Separate interface review: first install, upgrade failure, rollback, busy service and reboot evidence. Do not infer from package checksums. |

### Inference selector acceptance (#177)

Acceptance contract for J25 and J07/J12 recovery, associated with
[#177](https://github.com/o-psi/voyage/issues/177). The selectors are implemented; this contract is not interaction-test evidence.
See [current state](current-state.md) for behavior and verification limits.

- **Discover and choose:** clickable **Model**, **Thinking** and **Service**
  controls beside the composer show the selection and open a searchable modal.
  Type to filter, Up/Down to navigate, Enter to select, and Esc to cancel without
  changing settings or losing composer text.
- **One path:** `/model`, `/thinking` and `/service` open the corresponding picker.
  Typed arguments use the same validation, confirmation and submission path as
  clicks; they do not bypass safeguards.
- **Defaults and model changes:** `inherit` clears the corresponding explicit
  override and resolves an advertised catalog default when present, otherwise
  leaving the value provider-managed/unknown. `/service default` is explicit:
  standard on the OpenAI API, suppression of catalog tier selection on ChatGPT.
  Show catalog defaults separately from user overrides and actual response tiers. Changing model with Thinking or Service overrides requires
  explicit confirmation: reset both overrides atomically, or explicitly keep them
  for runtime/provider compatibility validation. Show what will be cleared; cancel
  retains the old model and overrides. Never silently reset them or silently carry
  incompatible overrides to the new model.
- **Capabilities:** offer choices from live capabilities for the selected
  model/provider on the executing host. Distinguish unknown or unavailable data
  from known unsupported choices. Unknown typed values remain subject to runtime
  validation, not an assurance of support. Local configuration drafts use supervised account-scoped catalog discovery;
  while unavailable, choices are transport suggestions, never entitlement claims. Remote drafts retain
  executing-host settings. Surface refusal without claiming a change.
- **Pending and recovery:** durably retain the pending action and preserve composer
  text across disconnect/restart. Distinguish pending from confirmed settings and
  prevent conflicting edits while the outcome is unknown. Helm automatically reconciles the
  original action without replaying it or replacing its identity. Definite refusal
  preserves text and returns to an actionable editing state.
- **Active turns:** accepted changes apply to the next turn only. Say so while a
  turn is active; its model, thinking and service do not change midway.

Executable acceptance must cover click/slash parity, search and keyboard cancellation,
default clearing, model-reset confirm/cancel, live versus unknown capabilities,
runtime refusal, automatic durable pending recovery, and active-turn timing. Preserve
a nonempty composer throughout; record actual results separately from this plan.

### Reconcile inventory completeness

Before each release, review both directions: every advertised user intention must
have a journey, and every reachable command/action must map to a journey or an
explicitly documented machine-only operation. Do not silently exclude a CLI-only
feature from the first-release UX promise. Review all subcommands beneath the
families above; family membership alone is not proof of complete coverage.

The current source anchors are:

- [TUI actions](https://github.com/o-psi/voyage/blob/main/helm/src/process_client/ui/actions.rs),
  [input](https://github.com/o-psi/voyage/blob/main/helm/src/process_client/ui/input.rs),
  [controls](https://github.com/o-psi/voyage/blob/main/helm/src/process_client/ui/controls.rs),
  [lifecycle](https://github.com/o-psi/voyage/blob/main/helm/src/process_client/ui/lifecycle.rs),
  [interactions](https://github.com/o-psi/voyage/blob/main/helm/src/process_client/ui/interactions/mod.rs) and
  [terminals](https://github.com/o-psi/voyage/blob/main/helm/src/process_client/ui/terminals.rs).
- [Connected CLI](https://github.com/o-psi/voyage/blob/main/helm/src/process_client/cli.rs),
  [administration](https://github.com/o-psi/voyage/blob/main/helm/src/process_client/admin/args.rs),
  [top-level CLI](https://github.com/o-psi/voyage/blob/main/helm/src/cli.rs), [installer](../installer/README.md).
- [Public Vessel API](https://github.com/o-psi/voyage/blob/main/crates/voyage-protocol/src/vessel.rs) and [private runtime IPC](https://github.com/o-psi/voyage/blob/main/crates/voyage-protocol/src/process/types.rs).

Runtime history/message/output commands map to J10/J11/J29; health, snapshots,
events and decisions supply J06/J08/J14–J16. Submit/steer/receipt/cancel map to
J08/J09/J12/J13. Controls/operator execution map to J20–J25/J32/J38; configuration
and lifecycle map to J21/J25–J28/J31. Workflow, GitHub and assignment commands map
to J24/J39/J35. Terminal operations map to J17–J19. Vessel catalogue/start/import/
branch/stop/restart/recovery map to J01–J06/J26/J31/J37. Grants, trust, participant,
and transfer commands map to J02/J33–J35. Internal forwarding and chunk
transport do not need standalone buttons, but their failures still need a journey.

The existing opt-in F6 shared local browser and direct image paste are current
journeys, not deferred browser-console plans. Their bounds and focused evidence
are in [local browser](local-browser.md) and [images](multimodal-implementation.md).
Voice and unsupported native platforms are not added to the release promise by
historical target plans. Unresolved scope is not an approved exclusion.

## State, input and layout coverage

For each journey, mark every state **exercised**, **not exercised**, or **not
applicable with reason**. The minimum state vocabulary is: unavailable/disabled,
empty, loading, ready, editing, submitting, admitted, running, waiting for human,
success, refusal, failure, timeout, cancellation requested, cleanup unresolved,
cleanup observed, disconnected, reconnected/stale and outcome unknown.

Do not require a meaningless Cartesian product. Every meaningful transition needs
coverage; authority, privacy and effectful actions require the relevant adverse
transitions on each supported route. In particular: submit then disconnect; cancel
then observe late output; switch voyage while a response is in flight; expire a
request while typing; resize between displaying and acting; change incarnation
between selecting and attaching; lose the receipt after an external effect.

Exercise these view/input families:

| Dimension | Required examples |
| --- | --- |
| Size | 80x24 baseline; 120x32 and 180x48 wide; 40x18 constrained; shrink below supported size and expand again. Essential controls must remain visible or the action must be disabled with a useful resize message. |
| Content | Empty, one item, many items; long names/paths; multiline input; long unbroken strings; tables/code; CJK and combining graphemes; terminal controls; structured payloads. |
| Input | Keyboard-only discovery and return paths, focus indicators, paste without submission, repeat keys without accidental consent, PageUp/PageDown, wheel scrolling where advertised. |
| Environment | Color and NO_COLOR; standard fonts without private-use icons; local HTTP/scoped HTTPS routes as promised; actual native platforms separately. |
| Concurrency | Multiple voyages and requests, background completion, changing inventory, disconnected host, permission changes, stale selection. |

A mocked screenshot can help design a missing state; label it **design only**.
An actual capture proves appearance for its recorded environment, not all fonts,
transports, platforms or runtime outcomes. A static image cannot establish focus,
keyboard routing, privacy or recovery.

## Executable evidence procedure

1. Select the release journeys and supported environments explicitly on #14. Assign
   an owner to each row and link its implementation/gap issue. Keep missing paths
   visible; do not mark them done because a generic JSON request can invoke them.
2. Specify expected screens, controls and transitions before fixing each journey.
   Build a review gallery from real renders; use clearly labeled designs only for
   absent states. Review language, density, focus and recovery alongside appearance.
3. Exercise an isolated real Helm -> Vessel -> voyage flow using harmless local
   programs and private temporary data. Use approved providers/budgets only when
   live provider evidence is required. Do not touch real user secrets or publish
   their console contents in captures.
4. Record the exact commit/binary hash, OS/terminal/font, dimensions, route, initial
   state, steps, expected and observed outcomes, capture paths, cleanup and issue.
   For input privacy use a dummy marker and inspect persistence; never record an
   actual password. Failures are findings, not passing evidence.
5. Use focused executable checks for discovery, exact input destination, adverse
   outcomes, bounded rendering and preserved drafts. Human usability research may
   be performed later, but is not required for this resolution. Never mark an
   unexecuted case passed merely because a screen or command exists.
6. Repeat affected scenarios after changes. Freeze exact release evidence and
   close only verified scope. Complete the separate build/packaging gates.

Use this record for each scenario (separate rows for different outcomes):

| Field | Record |
| --- | --- |
| Journey and scenario | Jxx, named outcome/transition, linked issue |
| Build and environment | Commit, binary hash, OS, terminal/font, size, route |
| Setup and actions | Reproducible starting state, inputs and executable assertions |
| Expected / observed | Visible screen, focus/input destination and runtime outcome |
| Evidence | Capture/recording paths and narrow/wide variants; sanitized local resource observations |
| Functional result | Verified / failed / not exercised; explicit limits |
| Scope | Supported behavior, dependency ownership and explicit exclusions |
| Remaining gap | Issue, owner and blocking scope |
| Cleanup | Owned processes/resources and observed cleanup or retained obligations |

The delivery decision reports **inventory coverage** and **functional scenario
coverage** separately. Every agreed journey must be accounted for and every
blocking executable gap must be resolved before full closure. Current user
instructions waive human testing and product acceptance, not functional behavior,
authority or privacy. Do not hide a failed row to improve coverage.

Focused Rust and offline process tests exist. They are not the removed broad test
suite and establish only their recorded source and environment. Preserve removed
root scripts/tests and earlier evidence; do not restore them implicitly. New
verification for changed journeys is scoped implementation work, not a claim of
whole-product certification.

## Remaining dependency ownership

- **#14:** general operator UX, unsupported structured tool forms and large-inventory
  paging; objective end-to-end journey evidence.
- **#10 / #200:** connection and provider credential protection/recovery and native
  storage. Named account/device-sign-in UI is delivered in #213. Credentials remain on
  the executing host; Helm does not become a remote provider credential store.
- **#70 / #71:** broader policy editing and resource/usage/budget contracts. A
  configuration path or cleanup count is not a complete editor/dashboard.
- **#32:** private-terminal structured cells, child-mode input, restoration and
  application fidelity. F3 attachment foundations are not that completion.
- **#77 / #79 / #21:** participant/scope/transfer and disclosure/decision authority;
  current in-app connections do not substitute for these distinct contracts.
- **#18 / #198:** packaging, deployed service/reboot/backup and public TLS evidence.
- **#63 / #75:** executable extension/SDK journeys; existing declarative tooling
  does not complete those backend contracts.

These are concrete ownership boundaries, not automatic exclusions from #14's
umbrella. Full issue closure requires their retained interface obligations to be
implemented and verified or an explicit scope decision. No historical visual
sign-off requirement may be used as a substitute blocker.

## Objective Linux verification for the operator-journey delivery

The focused `voyage/tests/ui_journeys.py` pass completed **11 assertion groups**
using development Helm/Vessel/Voyage binaries and isolated temporary HOME/XDG
storage. It uses explicit native loopback Chat Completions with
`api_key_required = false` and a synthetic named host account for the TUI's
account-selection contract; no real account, implicit ChatGPT login or paid
provider was used. Binary SHA-256 identities:

| Program | SHA-256 |
| --- | --- |
| Helm | `95f205d18fd1da2052948ce7dca32b7f65047286a7d2ebad9539ccabd5111609` |
| Vessel | `988dc36e6cc1f2aace4398af3ba34c5b59ea8dae6b31fdd60d9f85a03f36c7ab` |
| Voyage | `11f2ab49d4fab742623be854b1011149881456e9276d281ed704f63bfa928177` |

Observed assertions cover:

- 40×18, 80×24 and 120×32 startup/composer/navigation, exact Unicode and multiline
  drafts, empty-search Enter without submission, and NO_COLOR without emitted
  foreground/background/underline color SGR. Private account controls retain
  their explicit 44×22 minimum and Escape path; this does not claim account
  editing works at 40×18.
- Distinct simultaneous Voyage owners and canonical histories, unchanged prompts,
  detach without cancellation, reconnect and exact named selection.
- Actions branch of the selected canonical history and archive of only that child.
- Real approval review ignoring pasted consent, retaining the same decision over
  reconnect, then explicit Escape denial with zero file effect.
- Idle typed `read_file` and task creation without model inference; persisted task
  names available in management forms after reconnect, without UUID input.
- Named workflow selection, exact-digest trust, executing-host preview, optional
  private-reference parity and one submission. A synthetic private canary is
  absent from provider requests and persisted files, including PTY output.
- No fixture-owned Voyage PIDs remain; all seven Helm processes and the supervisor
  exit successfully, and the provider thread is joined. This is observed local
  process cleanup, not rollback of arbitrary external effects.

The focused Helm UI Rust run passed **65 tests**, including field/schema bounds,
rendered-review gates, named identities, grapheme-safe editing, late lifecycle
responses not redirecting another view, and private workflow invalidation retaining
an input quarantine until explicit Escape. Workspace coverage and its exact source
identity are recorded separately in [coverage/latest.json](../coverage/latest.json).
Coverage is execution evidence, not proof of whole-product correctness.

Earlier failures were retained, not converted into successful runs: a clipped F2
hint at 40×18 (fixed without weakening its assertion); stale shared-target protocol
artifacts (workspace entrypoint timestamps refreshed, no cache deletion); a fixture
Unix-socket path exceeding Linux's limit (shortened private paths, no timeout
increase); premature fixture checks before a named branch/menu was rendered; and
an invalidated private workflow form returning input to the composer (fixed with
quarantine and a regression). The task fixture's initial nested API envelope was
corrected to the actual flat public contract. Logs and private fixture directories
are indexed under ignored `target/issue14-verification/`.

Rust formatting passed. **Strict Clippy was not passing** on the integrated base:
account/provider and transcript lints remain in the retained logs; they are not
silently suppressed or described as a successful strict gate. The new operator
bridge lint found during this pass was corrected. No public TLS deployment,
independent-principal positive-approval UI, native macOS/Windows, arbitrary terminal
application fidelity, all MCP schema forms, large-inventory paging or full broader
policy/resource editor certification is implied. Those retained obligations, not
historical human visual approval, prevent umbrella closure.
