#!/bin/sh
set -eu

root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
repos="$root/.local-git/repos"

exec git daemon \
  --reuseaddr \
  --verbose \
  --export-all \
  --enable=receive-pack \
  --base-path="$repos" \
  --listen=127.0.0.1 \
  --port=9418 \
  "$repos"
