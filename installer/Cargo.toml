[package]
name = "voyage-installer"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
anyhow.workspace = true
crossterm = "0.29"
ratatui = { version = "0.30", default-features = false, features = ["crossterm"] }
url = "2.5"

[target.'cfg(unix)'.dependencies]
signal-hook = "0.3"
